# Twitch Random Stream Chrome Extension

This Chrome extension allows users to generate random Twitch URLs to currently active streams.

## Dependencies

* FontAwesome -- Must be downloaded directly into root folder.
